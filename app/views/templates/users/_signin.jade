link(rel='stylesheet', href='/styles/sign_up.css')


.signin

	.error(ng-show="message")
		{{ message }}

	input(type='text', ng-model='existingUser.username', placeholder='username')

	input(type='password', ng-model='existingUser.password', placeholder='password')

	button(ng-click='signIn()') Sign In

	include social